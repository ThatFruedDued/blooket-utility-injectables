if (!blooketUtility.prefs.infiniteFactory) {
  blooketUtility.prefs.infiniteFactory = {
    title: "Infinite Factory",
    description: "Prevent the timer from ticking in solo factory games",
    value: true,
  };
}
